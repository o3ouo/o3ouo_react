useCallback은 함수를 기억(메모이제이션)해주는 도구.

 - React에서는 컴포넌트가 리렌더링될 때마다 함수도 새로 만들어지는데, 똑같은 함수를 매번 새로 만들지 않도록 해주는 게 useCallback.
 ex. useCallback = 저장 상자
 > 컴포넌트가 리렌더링될 때 함수가 새로 만들어질 필요가 없다면, 상자에서 이미 저장된 함수를 꺼내 씀.
 > 필요 이상으로 새로 만드는 걸 방지해서 성능이 좋아질 수 있음.
